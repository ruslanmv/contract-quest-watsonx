# Contract Quest

A static HTML5 Canvas platformer generated for the governed Matrix Designer → Matrix Builder → GitPilot → IBM watsonx.ai workflow test.

## Commands

```bash
make install
make build
npm run build
npm run verify
npm run smoke
```

The generated static game does not require runtime credentials. The local `.env` is only used by the build workflow and must not be committed.
