# Allowed changes

The AI coder may edit **only**:

- `frontend/index.html`

Protected control files (must NOT be modified by a coder batch unless the batch explicitly
allows it): `MATRIX_BLUEPRINT.yaml`, `MATRIX_STANDARDS.lock`, `MATRIX_ALLOWED_CHANGES.md`,
`MATRIX_ACCEPTANCE_CRITERIA.md`, `MATRIX_VALIDATION.md`, `.github/workflows/`.
